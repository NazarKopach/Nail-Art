export const allBookings = (state) => state.bookings.bookings.bookings;
export const isBookingsLoading = (state) => state.bookings.isLoading;
export const bookingsError = (state) => state.bookings.error;
